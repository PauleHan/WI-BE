{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "uri": "${process.env.DATABASE_URI || 'mongodb://wiki-prod:kdbfbWPt2x3VnQ2>@ds237333-a0.mlab.com:37333,ds237333-a1.mlab.com:37333/wiki-strapi?replicaSet=rs-ds237333'}",
        "host": "${process.env.DATABASE_HOST || 'ds237333-a0.mlab.com'}",
        "port": "${process.env.DATABASE_PORT || 37333}",
        "database": "${process.env.DATABASE_NAME || 'wiki-strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'wiki-prod'}",
        "password": "${process.env.DATABASE_PASSWORD || 'kdbfbWPt2x3VnQ2'}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || false}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}"
      }
    }
  }
}
